const {
  extractSizeFromTerminalOutputW,
} = require("./extractSizeFromTerminalOutputW");
const {
  extractFileFromTerminalOutputW,
} = require("./extractFileFromTerminalOutputW");
const { extractSizeFromFileW } = require("./extractSizeFromFileW");
const { formatPathW } = require("./formatPathW");

module.exports = {
  extractSizeFromTerminalOutputW,
  extractFileFromTerminalOutputW,
  extractSizeFromFileW,
  formatPathW,
};
